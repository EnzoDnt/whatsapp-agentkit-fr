Lis `AGENTS.md` en entier, puis exécute l'installation qu'il décrit.

Rappel de la règle centrale, qui prime sur tout le reste :

> Tu fais TOUT ce que tu es techniquement capable de faire.
> Tu ne sollicites l'utilisateur QUE pour ce qui est impossible autrement,
> et tu lui expliques à chaque fois pourquoi.

Avant de demander quoi que ce soit, vérifie dans cet ordre :
1. Un outil MCP peut le faire ? (Meta Developer Tools : apps, webhooks, doc, conformité)
2. Une commande peut le faire ? (`curl`, `gh`, `uv`, `brew`, `openssl`)
3. Tu peux le générer ou choisir un défaut raisonnable ?
4. Sinon seulement : demande, avec le bloc « 🔴 J'ai besoin de toi 30 secondes ».

Commence par l'étape 1 d'`AGENTS.md` : faire vivre l'agent dans le simulateur
local. L'utilisateur doit voir son agent répondre avant qu'on parle de Meta.

Parle français. Une question à la fois. Ne promets rien que tu n'aies vérifié.
